$("#comment_section").append("<%= escape_javascript render partial: "comments/edit", locals: {comment: @comment, product: @product} %>");
$("#comment<%=  @comment.id %>").hide()
$("#new_comment").hide();
