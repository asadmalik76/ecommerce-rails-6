
<div id="update_form">
<%= form_for [product, comment], remote: :true, method: :PUT do |f| %>
		<%= hidden_field_tag :authenticity_token, form_authenticity_token %>
		<%= f.text_field :content, class: "form-control" %>
		<%= f.submit 'Update' %>
	<% end %>
</div>
